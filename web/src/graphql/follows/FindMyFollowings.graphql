query FindMyFollowings($data: FindFollowersInput!) {
  findMyFollowings(data: $data) {
    followings {
      createdAt
      followingId
      following {
        username
        avatar
      }
    }
    totalCount
  }
}
